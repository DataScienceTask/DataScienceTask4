version: "3.9"

services:
  test:
    build: .
    volumes:
      - .:/app
    command: pytest -v

  example:
    build: .
    volumes:
      - .:/app
    command: python -c "from unique_element import find_unique; print(find_unique([1,2,2,3,3]))"
